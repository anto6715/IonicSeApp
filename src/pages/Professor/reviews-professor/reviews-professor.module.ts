import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ReviewsProfessorPage } from './reviews-professor';

@NgModule({
  declarations: [
    ReviewsProfessorPage,
  ],
  imports: [
    IonicPageModule.forChild(ReviewsProfessorPage),
  ],
})
export class ReviewsProfessorPageModule {}
